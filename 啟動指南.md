# 快速啟動指南

## 問題排除

如果遇到 `streamlit` 命令找不到的錯誤,請使用以下方式運行:

### 方法 1: 使用 Python 模組方式 (推薦)
```bash
python -m streamlit run stock_analysis_app.py
```

### 方法 2: 重新安裝 Streamlit
```bash
pip install streamlit --no-deps
pip install -r requirements.txt
```

### 方法 3: 檢查安裝
```bash
pip list | findstr streamlit
```

## 常見錯誤解決

### pyarrow 編譯失敗 ❌
- **原因**: Windows 環境缺少 cmake 編譯工具
- **影響**: 不影響系統核心功能
- **解決**: 可忽略此錯誤,直接運行程式

### Streamlit 命令找不到 ❌
- **原因**: Streamlit 未正確安裝或不在 PATH 中
- **解決**: 使用 `python -m streamlit run stock_analysis_app.py`

## 完整安裝步驟

```bash
# 1. 進入專案目錄
cd "c:\Users\trist\Desktop\Google Antigravity\AI股票趨勢分析系統"

# 2. 安裝核心套件 (不安裝 pyarrow)
pip install streamlit requests pandas numpy plotly google-generativeai --no-cache-dir

# 3. 運行程式
python -m streamlit run stock_analysis_app.py
```

## API 金鑰準備

執行前請先準備:
1. **FMP API Key**: https://site.financialmodelingprep.com/developer/docs/
2. **Gemini API Key**: https://aistudio.google.com/app/apikey
